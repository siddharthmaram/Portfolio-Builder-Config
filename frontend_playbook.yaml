---
- name: Setting Frontend Application
  hosts: local
  become: no

  vars:
    deployment_name: "frontend-deployment"
    app_namespace: "portfolio-frontend-namespace"
    app_name: "portfolio-frontend"
    image_name: "siddharthmaram/portfolio-frontend:latest"
    container_port: 80

    ingress_name: "frontend-ingress"
    ingress_host: "portfolio-builder.com"

  tasks:
    - name: Include Common Role
      include_role:
        name: common

    - name: Include Frontend Role
      include_role:
        name: frontend